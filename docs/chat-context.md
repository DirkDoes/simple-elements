# Chat context

```html
<se-chat-context icon="info" timestamp="Today">You joined the conversation.</se-chat-context>
```

Displays a centered context event between messages, such as a renamed group, pinned message, changed model, or navigation event. Set the message as child HTML or with the `content` attribute.

Use `variant` to change the marker color and `icon` to change its symbol.
