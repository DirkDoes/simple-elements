# Profile

```html
<se-profile name="Sarah Jenkins" subtitle="Lead Designer" initials="SJ" clickable></se-profile>
```

Inside a collapsed `se-sidebar`, the profile automatically reduces to its avatar.

Attributes: `name`, `subtitle`, `initials`, and `clickable`.
