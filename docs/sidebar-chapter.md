# Sidebar chapter

```html
<se-sidebar-chapter title="Workspace" collapsible>
  <se-sidebar-button label="Dashboard" icon="dashboard"></se-sidebar-button>
</se-sidebar-chapter>
```

`se-sidebar-chapter` replaces the old section wrapper and provides optional animated disclosure with `title`, `collapsible`, and `collapsed`.
