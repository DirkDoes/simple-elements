# Time picker

```html
<se-time-picker label="Start time" name="event[start_time]" value="09:30" step="900"></se-time-picker>
```

Uses a native `input[type="time"]`, so it submits normally in HTML and Ruby on Rails forms. `step` is measured in seconds.
