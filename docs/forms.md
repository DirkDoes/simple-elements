# Forms pattern

The examples page includes account details, project creation, article publishing, and environment settings forms. Together they demonstrate the form components in realistic combinations, including saved values, validation errors, disabled fields, standard submit actions, selectable split-button actions, and direct split-button actions.

The forms use native `<form>` elements and prevent submission only because they are interactive examples.
