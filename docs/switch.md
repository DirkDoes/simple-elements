# Switch

```html
<se-switch label="Enable notifications" name="notifications" checked></se-switch>
```

Attributes: `label`, `name`, `checked`, and `disabled`. Read the `checked` property; native change events bubble.
