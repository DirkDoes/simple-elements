# Sidebar brand

```html
<se-sidebar-brand label="Simple Elements" icon="dashboard" collapsible></se-sidebar-brand>
```

Attributes: `label`, `icon`, and `collapsible`. The collapse button controls the nearest `se-sidebar`.
