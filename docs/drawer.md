# Drawer

```html
<se-drawer id="notifications" title="Notifications">Drawer content</se-drawer>
<script>document.querySelector('#notifications').open()</script>
```

Methods: `open()` and `close()`. The drawer emits `close`; Escape and backdrop clicks close it.
