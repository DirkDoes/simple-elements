# Title

```html
<se-title level="page">Account</se-title>
<se-title level="section">Preferences</se-title>
<se-title level="card">Notifications</se-title>
<se-title level="sidebar">Components</se-title>
```

`level` accepts `page`, `section`, `card`, or `sidebar`.
