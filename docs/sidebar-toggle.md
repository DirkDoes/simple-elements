# Sidebar toggle

```html
<se-sidebar id="app-sidebar" closed>...</se-sidebar>
<se-sidebar-toggle for="app-sidebar"></se-sidebar-toggle>
```

`se-sidebar-toggle` renders a square menu button that opens or closes the targeted `se-sidebar`. The `for` attribute is the sidebar element ID; `label` optionally overrides the accessible button label.
