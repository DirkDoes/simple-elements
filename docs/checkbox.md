# Checkbox

```html
<se-checkbox label="Email me updates" name="updates" value="yes" checked></se-checkbox>
```

Attributes: `label`, `name`, `value`, `checked`, and `disabled`. Read the `checked` property; native change events bubble from the internal checkbox.
